# Local data folder

This folder is used by the local Node server to store private app data in `app-state.json`.

`app-state.json` can contain essays, settings, and API keys. It is intentionally ignored by Git and should not be committed to a public repository.
